<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="PKHeX.MAUI.Views.InventoryEditorPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Inventory Editor">

    <ScrollView>
        <StackLayout Padding="10" Spacing="10">
            
            <!-- Header -->
            <Grid BackgroundColor="LightBlue" Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Text="← Back" Clicked="OnBackClicked"/>
                <Label Grid.Column="1" x:Name="HeaderLabel" Text="Inventory Editor" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center"/>
                <Button Grid.Column="2" Text="Save" Clicked="OnSaveClicked" BackgroundColor="LightGreen"/>
            </Grid>

            <!-- Generation Info -->
            <Frame BackgroundColor="LightYellow" Padding="10">
                <StackLayout>
                    <Label Text="Save File Information" FontSize="16" FontAttributes="Bold"/>
                    <Label x:Name="StatusLabel" Text="Loading..." FontSize="12"/>
                </StackLayout>
            </Frame>

            <!-- Inventory Pouches -->
            <Frame BackgroundColor="LightGreen" Padding="10">
                <StackLayout>
                    <Label Text="Inventory Pouches" FontSize="16" FontAttributes="Bold"/>
                    <Label x:Name="PouchCountLabel" Text="Pouches: 0" FontSize="12"/>
                    
                    <!-- Sample item editing - First pouch only for demonstration -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Label Grid.Row="0" Grid.Column="0" Text="Item ID:" VerticalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="1" Text="Count:" VerticalOptions="Center"/>
                        <Label Grid.Row="0" Grid.Column="2" Text="Action:" VerticalOptions="Center"/>
                        
                        <Entry Grid.Row="1" Grid.Column="0" x:Name="ItemIDEntry" Keyboard="Numeric" Placeholder="Item ID"/>
                        <Entry Grid.Row="1" Grid.Column="1" x:Name="ItemCountEntry" Keyboard="Numeric" Placeholder="Count"/>
                        <Button Grid.Row="1" Grid.Column="2" Text="Set" Clicked="OnSetItemClicked" BackgroundColor="LightCyan"/>
                        
                        <Button Grid.Row="2" Grid.Column="0" Text="Max All" Clicked="OnMaxAllClicked" BackgroundColor="LightSalmon"/>
                        <Button Grid.Row="2" Grid.Column="1" Text="Clear All" Clicked="OnClearAllClicked" BackgroundColor="LightPink"/>
                    </Grid>
                </StackLayout>
            </Frame>

            <!-- Status -->
            <Frame BackgroundColor="LightGray" Padding="10">
                <StackLayout>
                    <Label Text="Status" FontSize="16" FontAttributes="Bold"/>
                    <Label x:Name="OperationStatusLabel" Text="Ready" FontSize="12" TextColor="Green"/>
                </StackLayout>
            </Frame>

        </StackLayout>
    </ScrollView>

</ContentPage>
