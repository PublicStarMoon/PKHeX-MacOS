<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="PKHeX.MAUI.Views.PokemonBoxPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Pokemon Box Editor">

    <Grid BackgroundColor="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Menu Bar -->
        <Grid Grid.Row="0" BackgroundColor="#2C3E50" Padding="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" x:Name="BackButton" Text="← Back" Clicked="OnBackClicked" 
                    Margin="0,0,15,0" BackgroundColor="#34495E" TextColor="White" 
                    FontSize="14" FontAttributes="Bold" CornerRadius="6"/>
            <Label Grid.Column="1" Text="Pokemon Box Editor" FontSize="20" FontAttributes="Bold" 
                   VerticalOptions="Center" TextColor="White"/>
            <Button Grid.Column="2" x:Name="SaveButton" Text="Save Changes" Clicked="OnSaveClicked" 
                    Margin="8,0" BackgroundColor="#27AE60" TextColor="White" 
                    FontSize="14" FontAttributes="Bold" CornerRadius="6"/>
            <Button Grid.Column="3" x:Name="ExportBoxButton" Text="Export Box" Clicked="OnExportBoxClicked" 
                    Margin="8,0" BackgroundColor="#3498DB" TextColor="White" 
                    FontSize="14" FontAttributes="Bold" CornerRadius="6"/>
        </Grid>

        <!-- Box Selection and Info -->
        <Grid Grid.Row="1" Padding="15" BackgroundColor="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" Text="Box:" VerticalOptions="Center" Margin="0,0,15,0" 
                   FontSize="16" FontAttributes="Bold" TextColor="#2C3E50"/>
            <Picker Grid.Column="1" x:Name="BoxPicker" SelectedIndexChanged="OnBoxChanged" 
                    BackgroundColor="#ECF0F1" TextColor="#2C3E50" FontSize="14"/>
            <Label Grid.Column="2" x:Name="BoxCountLabel" Text="30/30" VerticalOptions="Center" 
                   Margin="15,0" FontSize="14" FontAttributes="Bold" TextColor="#7F8C8D"/>
            <Button Grid.Column="3" x:Name="ClearBoxButton" Text="Clear Box" Clicked="OnClearBoxClicked" 
                    BackgroundColor="#E74C3C" TextColor="White" FontSize="14" 
                    FontAttributes="Bold" CornerRadius="6"/>
        </Grid>

        <!-- Pokemon Box Grid -->
        <ScrollView Grid.Row="2" Padding="15" BackgroundColor="#F8F9FA">
            <Grid x:Name="PokemonGrid" RowSpacing="8" ColumnSpacing="8">
                <!-- Grid will be populated programmatically -->
            </Grid>
        </ScrollView>

        <!-- Bottom Status Bar -->
        <Grid Grid.Row="3" BackgroundColor="#34495E" Padding="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" x:Name="StatusLabel" Text="Select a Pokemon to edit" 
                   VerticalOptions="Center" TextColor="White" FontSize="14"/>
            <Button Grid.Column="1" x:Name="AddPokemonButton" Text="+ Add Pokemon" 
                    Clicked="OnAddPokemonClicked" BackgroundColor="#27AE60" TextColor="White" 
                    FontSize="14" FontAttributes="Bold" CornerRadius="6"/>
        </Grid>
    </Grid>

</ContentPage>
